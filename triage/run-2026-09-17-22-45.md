
===== TRIAGE 2026-09-17 22:45:53 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m✱ [0mGlob "**/*roobet*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/roobet.md
[0m→ [0mRead triage-prompt.txt
[0m→ [0mRead triage-output.txt
[0m→ [0mRead probe-results.md[90m [limit=100][0m
[0m✱ [0mGlob "leads/*"[90m 7 matches[0m
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead knowledge
[0m→ [0mRead state_bigpickle.json[90m [limit=50][0m
[0m→ [0mRead knowledge/index.md
[0m→ [0mRead inventory/roobet.md[90m [offset=444][0m
The `LEADS:` section in the prompt is empty — no leads were provided to evaluate. Let me pull the most recent actionable findings from the inventory to extract candidate leads.
The `LEADS:` section in the prompt is empty. Extracting the 7 most actionable candidate leads from the inventory/probe results and applying the 7-Question Gate:

---

## Lead 1: GraphQL Anonymous WS Introspection (roobet.com/_api/graphql)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** — roobet.com is explicitly in-scope ("all company-owned infrastructure") |
| Q2 Reachable | **YES** — anonymous graphql-transport-ws upgrade succeeds (101 + connection_ack, no socketToken required) |
| Q3 Impact | **YES** — full schema introspection disclosed: Query 28, Mutation 26, Subscription 7; User type exposes 64 fields incl. email, KYC PII, financial balances, auth tokens, role flags |
| Q4 Provable | **YES** — single read-only WS connect + introspection query; non-destructive |
| Q5 Novel | **HOLD** — GraphQL introspection disclosure is commonly reported; severity capped by resolver-level NOT_AUTHENTICATED. Need to check if this specific anonymous WS path + full schema is already known to Roobet |
| Q6 Not rejected | **YES** — not on out-of-scope list; not DoS, not social engineering, no customer data exposed during testing |
| Q7 Reasonable triager | **HOLD** — informational-only; resolver auth enforced on data-plane; unlikely to be accepted as standalone finding unless combined with an auth bypass |

**Verdict: HOLD** — True finding, but resolver auth caps impact. Novel if the anonymous WS path was previously gated; reportable only if a reviewer finds pre-auth schema disclosure sufficient. No standalone bounty expected.

---

## Lead 2: GraphQL Pre-Auth Mutation Execution (13/26 mutations return ISE, not 401)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** — roobet.com/_api/graphql is in-scope |
| Q2 Reachable | **YES** — HTTP POST to _api/graphql with mutation body succeeds without auth |
| Q3 Impact | **YES** — mutations execute resolver logic pre-auth (INTERNAL_SERVER_ERROR, not 401/NOT_AUTHENTICATED); could indicate auth bypass if any mutation returns data |
| Q4 Provable | **YES** — single HTTP POST per mutation; read-only side-effect (errors only, no state change confirmed) |
| Q5 Novel | **YES** — mutations like `placeBet`, `claimBonus`, `updatePassword`, `sendTip` executing pre-auth is unusual; most APIs reject at middleware level |
| Q6 Not rejected | **YES** — not DoS, no customer data exposed, not social engineering |
| Q7 Reasonable triager | **YES** — pre-auth mutation execution is a real security finding (auth bypass candidate); even with ISE, it proves the resolver runs without auth check |

**Verdict: VALID** — Pre-auth mutation execution is a genuine finding.

**Proof steps:** `curl -X POST https://roobet.com/_api/graphql -H "Content-Type: application/json" -d '{"query":"mutation{placeBet(amount:1,game:\"crash\"){id}}"}'` — observe 400+ISE, not 401.

**Impact:** Medium — resolver logic executes without auth; if any mutation returns data or changes state without auth, severity escalates to High-Critical. Current evidence: ISE only, no data/state change confirmed.

**CVSS 3.1:** 5.3 (Medium) — AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N

**Reporting channel:** bugs.olivermaicher.eu

---

## Lead 3: GraphQL Mutation Validation Error Disclosure (InputType/enum leakage)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** |
| Q2 Reachable | **YES** — anonymous HTTP POST triggers validation errors revealing schema internals |
| Q3 Impact | **YES** — leaks exact InputType shapes, required fields, enum values, custom scalar formats for all 26 mutations |
| Q4 Provable | **YES** — single HTTP POST with intentionally malformed mutation body; non-destructive |
| Q5 Novel | **HOLD** — validation error disclosure is commonly reported; severity low |
| Q6 Not rejected | **YES** |
| Q7 Reasonable triager | **HOLD** — informational; assists attacker recon but no direct security impact |

**Verdict: HOLD** — Useful recon but low severity. Combine with Lead 2 for a stronger report.

---

## Lead 4: GraphQL Subscription Error Disclosure (kycUpdated/polymarketOrders)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** |
| Q2 Reachable | **YES** — anonymous WS subscription triggers JS error |
| Q3 Impact | **YES** — internal error `Cannot read properties of null (reading 'user')` discloses server architecture (resolver accesses session user directly) |
| Q4 Provable | **YES** — single WS subscribe; non-destructive |
| Q5 Novel | **HOLD** — error message disclosure is common; severity very low |
| Q6 Not rejected | **YES** |
| Q7 Reasonable triager | **NO** — JS runtime error disclosure without data leak is informational at best |

**Verdict: INVALID** — Insufficient impact. Internal error message without data exposure is out-of-scope under "descriptive error messages" exclusion.

---

## Lead 5: Anonymous Socket.IO Broadcast (global bet feed)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** |
| Q2 Reachable | **YES** — engine.io polling handshake succeeds with only `Origin: https://roobet.com`; anonymous namespace `/` broadcasts `new_bet`, `settingsUpdated`, withdraw flags |
| Q3 Impact | **YES** — live global bet feed broadcast to unauthenticated connections could leak real-time betting activity |
| Q4 Provable | **YES** — single HTTP GET + socket.io connect; non-destructive |
| Q5 Novel | **HOLD** — anonymous broadcast of public game activity is common in crypto casinos; may be by-design |
| Q6 Not rejected | **YES** |
| Q7 Reasonable triager | **HOLD** — if broadcasts are intentionally public (e.g., provably fair transparency), this is not a vuln; needs verification |

**Verdict: HOLD** — Could be by-design (crypto casinos often broadcast bets publicly). Verify if any non-public data (user IDs, balances) leaks in broadcast. If only aggregate/public data, INVALID.

---

## Lead 6: Settings/Get Config Dump (client IP, sessionId, restricted countries)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** |
| Q2 Reachable | **YES** — HTTP GET returns 200 JSON without auth |
| Q3 Impact | **YES** — exposes client IP, per-request sessionId, countryCode, restrictedCountries (35 entries), withdraw flags, globalStats.allTimeNumBets (~15.4B) |
| Q4 Provable | **YES** — single HTTP GET; non-destructive |
| Q5 Novel | **NO** — already accepted in knowledge base (2026-09-04 onwards); known finding |
| Q6 Not rejected | **YES** |
| Q7 Reasonable triager | **NO** — already known; config dump with public-ish operational data is low severity |

**Verdict: INVALID** — Already known and accepted; low severity; operational intelligence only, no customer/financial data.

---

## Lead 7: Admin Endpoints on Low-Gate Surface (_api/admin/users, _api/admin/stats)

| Gate | Verdict |
|------|---------|
| Q1 Scope | **YES** |
| Q2 Reachable | **YES** — HTTP GET returns 401 (not 404), confirming endpoint existence on unauthenticated surface |
| Q3 Impact | **YES** — admin endpoints accessible without Cloudflare bot-gate; if auth bypass possible, full admin access |
| Q4 Provable | **YES** — single HTTP GET; non-destructive |
| Q5 Novel | **NO** — already accepted in knowledge base (2026-09-04 onwards); known finding |
| Q6 Not rejected | **YES** |
| Q7 Reasonable triager | **NO** — already known; 401 with no confirmed bypass is informational |

**Verdict: INVALID** — Already known; 401 auth-gated with no bypass confirmed. Reportable only if auth bypass is found.

---

## Summary

| Lead | Verdict | CVSS | Notes |
|------|---------|------|-------|
| GraphQL Anonymous WS Introspection | **HOLD** | — | Resolver auth caps impact; informational |
| Pre-Auth Mutation Execution | **VALID** | 5.3 Medium | Genuine finding; report to bugs.olivermaicher.eu |
| Mutation Validation Error Disclosure | **HOLD** | — | Combine with pre-auth mutations for stronger report |
| Subscription Error Disclosure | **INVALID** | — | Descriptive error message, out-of-scope |
| Anonymous Socket.IO Broadcast | **HOLD** | — | May be by-design; verify data sensitivity |
| Settings/Get Config Dump | **INVALID** | — | Already known; low severity |
| Admin Endpoints on Low-Gate | **INVALID** | — | Already known; 401 auth-gated |

**Recommended report:** Lead 2 (Pre-Auth Mutation Execution) is the only standalone VALID finding worth submitting. Combine with Lead 1 (anonymous introspection) and Lead 3 (validation error disclosure) for a comprehensive GraphQL security report.
