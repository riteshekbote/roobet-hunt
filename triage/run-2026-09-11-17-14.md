
===== TRIAGE 2026-09-11 17:14:32 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*LEAD*"[90m 0 matches[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead probe-results.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.txt
Now let me read the remaining lead files and new-leads.txt:
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=384][0m
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=464][0m
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*roobet*"[90m 2 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/roobet.md
I have enough context to evaluate. Here are all distinct, deduplicated hypotheses from the bot output, each run through the 7-Question Gate:

---

## LEAD 1: Anonymous GraphQL Introspection + Schema Disclosure on `roobet.com/_api/graphql`

**Asset:** `wss://roobet.com/_api/graphql` + `https://roobet.com/_api/graphql`  
**Findings:** Anonymous `graphql-transport-ws` upgrade (101 + `connection_ack`, no `socketToken`); full `__schema` introspection succeeds (Query 28 / Mutation 26 / Subscription 7). User type exposes 64 fields incl. email, KYC PII (firstName/lastName/dob/address/phone), financial (balances/withdrawn/deposited/lifetimeValue), auth tokens. All 26 mutations pass GraphQL validation anonymously, leaking exact InputType shapes and required fields.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` is "All company-owned infrastructure" in scope |
| Q2 Reachable | Yes | Anonymous WS upgrade + introspection, no auth required |
| Q3 Impact | Yes | Full API surface mapping → enables targeted IDOR/mass-assignment/auth bypass; 64-field User type with PII/financial fields is info disclosure even if resolvers enforce `NOT_AUTHENTICATED` |
| Q4 Provable | Yes | `wss://roobet.com/_api/graphql` → WS connect → `connection_init` → `connection_ack` → introspection query. Read-only, non-invasive. |
| Q5 Novel | Yes | Not on always-rejected list; anonymous introspection on production GraphQL is a real finding |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | Debatable | Resolvers enforce `NOT_AUTHENTICATED` at data layer — but schema/introspection disclosure alone is reportable per many programs. CSW entry says "exposure of customer/employee/financial/auth data during testing" is out of scope, but this is schema metadata not actual data. |

**Verdict: HOLD** — Schema disclosure alone may be accepted but the data-layer enforcement reduces real impact. The mutation validation error disclosure (InputType shapes) adds value. Needs judgment: if the program rewards API surface mapping aids, this is LOW-MEDIUM; if they require data exfil, it's informational-only.

**If VALID:** Read-only WS introspection via browser devtools. Impact: API surface mapping. CVSS 3.1: 5.3 (Medium) — AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N. Report via bugs.olivermaicher.eu.

---

## LEAD 2: GraphQL Mutation Resolver Pre-Auth Execution (`liveRTPUpdate`)

**Asset:** `roobet.com/_api/graphql` (HTTP POST)  
**Findings:** Mutation `liveRTPUpdate` returns `200 + "No user with that id" + INTERNAL_SERVER_ERROR` without authentication. Resolver executes pre-auth on HTTP channel, leaking internal error state.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | Anonymous HTTP POST to GraphQL endpoint |
| Q3 Impact | Low | Error message is generic ("No user with that id"); `INTERNAL_SERVER_ERROR` leaks execution path but no data. Pre-auth execution of mutations that should require auth is a defense-in-depth issue. |
| Q4 Provable | Yes | `POST https://roobet.com/_api/graphql` with mutation body → 200 response. Read-only. |
| Q5 Novel | Possibly | May have been patched (later runs show 400 instead of 200), suggesting program already addressed it |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | Weak | Program excludes "descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." This is essentially a verbose error leak. Low standalone value. |

**Verdict: HOLD** — The pre-auth mutation execution is interesting but the error disclosure is explicitly out-of-scope ("Descriptive error messages"). If it can be chained to cause a state change, that's different, but evidence shows it just errors out.

---

## LEAD 3: Admin API Privilege Escalation on Low-Gate Surface (`_api/admin/*`)

**Asset:** `roobet.com/_api/admin/users`, `/_api/admin/stats`  
**Findings:** Admin endpoints exist on the low-gate Remix server (bypasses Cloudflare bot-management), return `401 Unauthorized` without auth. Auth mechanism is Express.js `connect.sid` session cookies. The critical question: does a normal user session grant admin access? **Unverified** — requires a live session cookie.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | HTTP GET, unauthenticated returns 401 (not 404) — endpoint exists |
| Q3 Impact | High (if confirmed) | Admin user enumeration/stats → potential priv-esc |
| Q4 Provable | **No** | Needs a valid user session (`connect.sid`) to test; cannot verify without signing up. Auth is server-side. |
| Q5 Novel | Possibly | Admin routes on a non-bot-gated surface is interesting |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | Cannot prove the vulnerability exists without a session. Current evidence is only 401 — this is expected secure behavior (auth-gated). A triager would reject "admin endpoint returns 401" as a finding. |

**Verdict: INVALID** — 401 on unauthenticated access is expected behavior, not a vulnerability. Without proof that a normal user session bypasses role checks, this is an unproven hypothesis, not a finding.

---

## LEAD 4: Affiliate Endpoint IDOR/BOLA (`_api/affiliate/get`)

**Asset:** `roobet.com/_api/affiliate/get`  
**Findings:** Endpoint returns 401 unauthenticated. Parameter tests (`?user_id=`, `?affiliate_code=`) also return 401. Requires a valid session to test if parameters reflect another user's data.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | HTTP GET, but auth-gated (401) |
| Q3 Impact | Medium (if confirmed) | Cross-user affiliate data leakage |
| Q4 Provable | **No** | Needs valid session to test parameter influence |
| Q5 Novel | Possibly | Endpoint on non-bot-gated surface |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | 401 on unauthenticated access is normal. No evidence of IDOR. Cannot confirm parameter reflects cross-user data. |

**Verdict: INVALID** — Same issue as Lead 3: 401 is expected behavior. No proof of IDOR exists. Unproven hypothesis.

---

## LEAD 5: Anonymous Socket.IO Realtime Bet Feed (`_api/socket.io`)

**Asset:** `roobet.com/_api/socket.io`  
**Findings:** Engine.io polling handshake succeeds with only `Origin: https://roobet.com`. Anonymous connect to default namespace `/` pushes live global `new_bet` feed and `settingsUpdated` broadcast. Origin-gated (403 without `Origin: https://roobet.com`).

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | Requires `Origin: https://roobet.com` header, but this is trivially settable from any origin (not a security control) |
| Q3 Impact | Low | Global broadcast of `new_bet` feed is public info (other players' bets are visible on the site). `settingsUpdated` broadcast and `withdraw` flags may leak operational config but this is likely intended behavior for the SPA. |
| Q4 Provable | Yes | Read-only subscribe via polling transport |
| Q5 Novel | Unlikely | Realtime bet feeds are common in crypto casinos and intended to be public |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | Global bet feeds are public by design on gambling sites. The Origin check is functioning correctly. No security impact. |

**Verdict: INVALID** — Global bet feed broadcast is intended public functionality, not a security issue.

---

## LEAD 6: Internal Config Leak via `_api/settings/get`

**Asset:** `roobet.com/_api/settings/get`  
**Findings:** Returns client IP, per-request 64-hex sessionId, countryCode/regionCountryCode, full restrictedCountries map (33 entries), restrictedRegion flags, 22 `disabled*Withdraw` flags, serverTime. Input reflection NEGATIVE (XFF, ?sessionId, Referer, Cookie all inert).

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | Anonymous GET, no auth required |
| Q3 Impact | Very Low | Exposed data is operational config likely intended for client consumption (geo-restrictions, feature flags). IP is the requester's own IP. SessionId rotates per-request (not a secret). Withdraw flags are client-side config. |
| Q4 Provable | Yes | `GET https://roobet.com/_api/settings/get` → 200 JSON |
| Q5 Novel | No | Config endpoints returning client-side settings are standard |
| Q6 Not rejected | Yes | Not on explicitly rejected list |
| Q7 Acceptable | **No** | Program excludes "Exposure of known public files or directories" and "Descriptive error messages." This is client-side config that the SPA consumes — it's intentionally returned. No customer/financial/auth data exposed. |

**Verdict: INVALID** — Client-side configuration endpoint returning settings the SPA needs. Not a security issue.

---

## LEAD 7: GraphQL Admin WS Ports (8087/8088) on `api.roobet.com`

**Asset:** `api.roobet.com:8087`/`:8088`  
**Findings:** Client bundle embeds these ports but they are TCP-refused at Cloudflare edge. HTTP and WS upgrade on 443/8443 return 403.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `api.roobet.com` in scope |
| Q2 Reachable | **No** | Ports 8087/8088 unreachable; Cloudflare blocks WS upgrades on 443/8443 (403) |
| Q3 Impact | N/A | Cannot reach |
| Q4 Provable | **No** | Cannot confirm reachability |
| Q5 Novel | N/A | Unreachable |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | Cannot prove the service is exposed |

**Verdict: INVALID** — Unreachable. No finding.

---

## LEAD 8: Crash Game WebSocket Unauthenticated State (`crash-gs.roobet.com`)

**Asset:** `crash-gs.roobet.com` Socket.IO  
**Findings:** TLS WS upgrade stays open then app-layer timeout. Requires browser/WS tooling (HUMAN_ONLY). Hypothesis: game state channels may be reachable without auth.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `crash-gs.roobet.com` in scope (company gaming infra) |
| Q2 Reachable | Uncertain | WS upgrade connects but no evidence of data without auth |
| Q3 Impact | Low-Medium | Unauthorized game state read |
| Q4 Provable | **No** | HUMAN_ONLY, requires browser, cannot verify with passive probing |
| Q5 Novel | Possibly | Game server auth model unknown |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | Cannot prove the vulnerability exists |

**Verdict: INVALID** — Unproven hypothesis. No evidence of data exposure.

---

## LEAD 9: GCS Bucket Enumeration on `fs.roobet.com`

**Asset:** `fs.roobet.com`  
**Findings:** `?list-type=2` returns 301 to fullstory.com (not 200 with ListBucketResult). Bucket serves only FullStory JS assets.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `fs.roobet.com` in scope |
| Q2 Reachable | Yes | HTTP GET |
| Q3 Impact | No | 301 redirect — no listing possible |
| Q4 Provable | Yes, disproven | Probes confirm no listing |
| Q5 Novel | N/A | No finding |
| Q6 Not rejected | Yes | N/A |
| Q7 Acceptable | **No** | Bucket is not misconfigured |

**Verdict: INVALID** — Disproven by probe results.

---

## LEAD 10: Pusher Auth Endpoint Bypass

**Asset:** `roobet.com/pusher/auth`  
**Findings:** POST-only endpoint, returns 405 on test format, 200 SPA shell on GET. Requires valid session + exact body.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Partially | Route exists but POST-only, requires exact body |
| Q3 Impact | Medium (if bypassed) | Private channel access |
| Q4 Provable | **No** | Needs valid session + correct body format |
| Q5 Novel | Possibly | Session validation bypass is a real class |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | **No** | Cannot prove bypass exists |

**Verdict: INVALID** — Unproven hypothesis. No evidence of bypass.

---

## LEAD 11: ContentStack Preview Token Leak

**Asset:** `roobet.com` SPA bundle  
**Findings:** Delivery tokens public-by-design. Preview token might read draft CMS content.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | Token in client bundle |
| Q3 Impact | Very Low | Draft marketing content; program-excluded class (no customer/financial/auth data) |
| Q4 Provable | Partially | Would need to hit ContentStack API |
| Q5 Novel | No | ContentStack tokens in client bundles are public-by-design |
| Q6 Not rejected | Yes | Near-REJECTED class (public tokens) |
| Q7 Acceptable | **No** | Delivery tokens are public; preview tokens are a low-severity info disclosure for marketing content only |

**Verdict: INVALID** — Public-by-design tokens. Out-of-scope content type.

---

## LEAD 12: GraphQL User Type Full Schema Disclosure (64 fields incl. PII names)

**Asset:** `roobet.com/_api/graphql`  
**Findings:** Introspection reveals User type with 64 fields including `email`, `firstName`, `lastName`, `dob`, `address`, `phone`, `balances`, `withdrawn`, `deposited`, `lifetimeValue`, auth tokens.

| Q | Answer | Detail |
|---|--------|--------|
| Q1 Scope | Yes | `roobet.com` in scope |
| Q2 Reachable | Yes | Anonymous introspection |
| Q3 Impact | Medium | Schema disclosure reveals exact data model for PII/financial fields — enables targeted attacks even if data-layer enforces auth |
| Q4 Provable | Yes | Read-only introspection query |
| Q5 Novel | Yes | Full PII field schema on anonymous endpoint |
| Q6 Not rejected | Yes | Not on always-rejected list |
| Q7 Acceptable | Debatable | Same tension as Lead 1: schema disclosure is real but data is gated. Many programs accept this as informational/low. |

**Verdict: HOLD** — Same as Lead 1. Schema-level PII field disclosure is a real (low) finding. Roll into Lead 1 report if submitted.

---

## SUMMARY

| # | Lead | Verdict | Reason |
|---|------|---------|--------|
| 1 | Anonymous GraphQL Introspection + Schema Disclosure | **HOLD** | Real schema disclosure but resolvers enforce auth. May be accepted as informational. |
| 2 | Mutation Pre-Auth Execution (liveRTPUpdate) | **HOLD** | Pre-auth execution is real but error disclosure is explicitly out-of-scope. |
| 3 | Admin API Priv-Esc (_api/admin/*) | **INVALID** | 401 is expected behavior. No proof of bypass. |
| 4 | Affiliate IDOR (_api/affiliate/get) | **INVALID** | 401 is expected behavior. No proof of IDOR. |
| 5 | Anonymous Socket.IO Bet Feed | **INVALID** | Global bet feed is public by design. |
| 6 | Config Leak (_api/settings/get) | **INVALID** | Client-side config endpoint, intentionally public. |
| 7 | GraphQL Admin WS Ports (8087/8088) | **INVALID** | Unreachable (Cloudflare blocks). |
| 8 | Crash Game WS Unauthenticated State | **INVALID** | Unproven hypothesis, no data evidence. |
| 9 | GCS Bucket Enumeration (fs.roobet.com) | **INVALID** | Disproven by probe (301 redirect, no listing). |
| 10 | Pusher Auth Endpoint Bypass | **INVALID** | Unproven hypothesis. |
| 11 | ContentStack Preview Token Leak | **INVALID** | Public-by-design tokens, out-of-scope content. |
| 12 | GraphQL User Type 64-Field PII Schema | **HOLD** | Roll into Lead 1 if reporting. |

**Bottom line:** No lead reaches VALID. The strongest candidate is the **anonymous GraphQL introspection with full PII schema disclosure** (Leads 1+12), which is HOLD — it's a real finding but its severity depends on the program's appetite for schema-only disclosures when data-layer auth is enforced. I would NOT recommend submitting the others.
