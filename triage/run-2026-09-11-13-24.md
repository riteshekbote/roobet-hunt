
===== TRIAGE 2026-09-11 13:24:31 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*roobet*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*inventory*"[90m 0 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/roobet.md
[0m→ [0mRead leads/reposcan-roobet.md
[0m→ [0mRead inventory/roobet.md[90m [offset=445][0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*hunt*"[90m 0 matches[0m
Let me check the rest of the inventory file and look for any other lead files.
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead new-leads.txt
The `LEADS:` section at the end is empty — the bot didn't append any structured leads there. But the hunt data across all 6 lead files and the inventory contains extensive findings. I'll extract the distinct concrete findings and validate each.

---

## LEAD 1: GraphQL Anonymous Introspection via WebSocket — Full Schema Disclosure

**Finding:** `roobet.com/_api/graphql` accepts anonymous `graphql-transport-ws` upgrade (101 + `connection_ack`, no `socketToken`). Full `__schema` introspection succeeds: 28 queries, 26 mutations, 7 subscriptions. User type exposes 64 fields incl. email, KYC PII (firstName/lastName/dob/address/phone), financial (balances/withdrawn/deposited/lifetimeValue), auth tokens. Mutation validation errors leak exact InputType shapes. Subscriptions (`kycUpdated`, `polymarketOrders`) leak internal error strings.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | `roobet.com` is in-scope ("all company-owned infrastructure, gaming platforms, APIs, web applications") |
| Q2 Attacker-reachable | YES | Unauthenticated: WS upgrade succeeds without `socketToken`; introspection returns full schema |
| Q3 Real security impact | YES | Schema discloses full data model incl. PII/financial fields; enables targeted attacks on auth-gated resolvers; mutation input shapes leaked |
| Q4 Provable non-invasively | YES | `wss://roobet.com/_api/graphql` + `connection_init` + introspection query — read-only, <1 request |
| Q5 Novel/unreported | LIKELY | Not on any public disclosure; anonymous introspection on production GraphQL is uncommon |
| Q6 Not always-rejected | YES | Not on the rejection list (not stack trace, not banner grab, not OPTIONS/TRACE) |
| Q7 Reasonable triager accept | YES | Real API surface disclosure enabling further exploitation; non-trivial |

**Verdict: VALID**
- **Proof:** `wss://roobet.com/_api/graphql` → `{"type":"connection_init"}` → `{"type":"start","id":"1","payload":{"query":"{__schema{types{name,fields{name}}}}"} Introspection returns full schema including User type 64 fields.
- **Impact:** Full API schema disclosure including PII/financial data model enables targeted IDOR/mass-assignment attacks on auth-gated resolvers.
- **CVSS 3.1:** 5.3 (Medium) — AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N (info disclosure enabling further attack)
- **Channel:** bugs.olivermaicher.eu

---

## LEAD 2: Pre-Auth Mutation Execution — `liveRTPUpdate` Returns INTERNAL_SERVER_ERROR

**Finding:** `roobet.com/_api/graphql` HTTP POST mutation `liveRTPUpdate` executes resolver pre-auth, returning `200 "No user with that id" + INTERNAL_SERVER_ERROR` (later patched to `400 + INTERNAL_SERVER_ERROR`). Resolver executes without authentication gate.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | YES | Unauthenticated HTTP POST |
| Q3 Real security impact | MEDIUM | Resolver executes pre-auth; error message disclosure; but no data returned, auth check happens inside resolver |
| Q4 Provable non-invasively | YES | Single HTTP POST with mutation query |
| Q5 Novel/unreported | UNCERTAIN | May be known internally; pre-auth execution is real but severity limited |
| Q6 Not always-rejected | YES | Not on rejection list |
| Q7 Reasonable triager accept | DEBATABLE | Resolver executes but no data leaks; error was patched. Borderline — program might accept as "pre-auth mutation execution" or reject as "no data impact" |

**Verdict: HOLD**
- Reason: Resolver executes pre-auth (confirmed), but no sensitive data returned. Server patched error text post-discovery, suggesting awareness. Needs deeper investigation (e.g., other mutations with same pattern) to demonstrate impact.

---

## LEAD 3: Apollo GET Transport Enables CSRF Bypass

**Finding:** `roobet.com/_api/graphql` HTTP GET with Apollo `x-apollo-operation-name` header bypasses CSRF preflight. Cacheable URL creates proxy/referrer leakage advantages.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | YES | Unauthenticated GET |
| Q3 Real security impact | LOW-MEDIUM | CSRF bypass on GraphQL; but resolver auth gates data; only useful if combined with other vuln |
| Q4 Provable non-invasively | YES | Single GET request |
| Q5 Novel/unreported | UNCERTAIN | Apollo CSRF behavior is well-documented; specific to roobet's implementation may be novel |
| Q6 Not always-rejected | YES | CSRF on logout/forms-for-anonymous is rejected, but GraphQL CSRF is not listed |
| Q7 Reasonable triager accept | BORDERLINE | Program explicitly rejects "CSRF on forms available to anonymous users" and "CSRF on logout." GraphQL GET transport CSRF may fall under "descriptive headers" category. |

**Verdict: HOLD**
- Reason: Apollo GET CSRF is a known behavior pattern. Program's CSRF exclusions may cover this. Needs demonstration of concrete exploit chain (e.g., state-changing mutation via CSRF) to be accepted.

---

## LEAD 4: Admin Endpoints on Low-Gate `_api/*` Surface

**Finding:** `roobet.com/_api/admin/users` and `_api/admin/stats` return 401 Unauthorized. These admin endpoints bypass Cloudflare bot-gate entirely and exist on the same unauthenticated `_api/*` surface as `settings/get`.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | PARTIALLY | HTTP reachable without Cloudflare, but 401 auth-gated |
| Q3 Real security impact | LOW | 401 = properly auth-gated; no bypass demonstrated |
| Q4 Provable non-invasively | YES | GET request returns 401 |
| Q5 Novel/unreported | LOW | Admin endpoints behind 401 is expected behavior |
| Q6 Not always-rejected | YES | Not explicitly on rejection list |
| Q7 Reasonable triager accept | NO | 401 Unauthorized = properly protected. No auth bypass. Not a vulnerability. |

**Verdict: INVALID**
- Reason: 401 = auth working correctly. Admin endpoints being reachable but auth-gated is not a vulnerability. Would need auth bypass to be reportable.

---

## LEAD 5: Affiliate Endpoint with Parameter Parsing (Potential IDOR)

**Finding:** `roobet.com/_api/affiliate/get` returns 401 (not 404). Accepts `?user_id` and `?affiliate_code` parameters (returns 401 not 404, confirming parameter parsing). Potential IDOR if auth bypass discovered.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | PARTIALLY | HTTP reachable, 401 auth-gated |
| Q3 Real security impact | LOW | No auth bypass demonstrated; parameter parsing confirmed but data not leaked |
| Q4 Provable non-invasively | YES | GET with parameters returns 401 |
| Q5 Novel/unreported | UNCERTAIN | Parameter acceptance is interesting but 401 blocks exploitation |
| Q6 Not always-rejected | YES | Not on rejection list |
| Q7 Reasonable triager accept | NO | 401 = properly protected. Hypothetical IDOR requires auth bypass not demonstrated. |

**Verdict: INVALID**
- Reason: 401 = auth working. Parameter acceptance without data leak is not a vulnerability. Would need auth bypass + cross-user data access.

---

## LEAD 6: `_api/settings/get` Internal Config Disclosure

**Finding:** `roobet.com/_api/settings/get` returns 200 with client IP, per-request 64-hex sessionId, countryCode, restrictedCountries map (35 entries), withdraw flags, geo inference, serverTime.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | YES | Unauthenticated GET, 200 |
| Q3 Real security impact | LOW | Operational config/intelligence; no customer/financial/auth data; matches "descriptive headers" class |
| Q4 Provable non-invasively | YES | Single GET request |
| Q5 Novel/unreported | LOW | Config endpoints leaking operational data are common |
| Q6 Not always-rejected | YES | "Descriptive error messages or headers" is rejected; this is a config endpoint, but the data disclosed is server-side operational info (not stack traces or banners) |
| Q7 Reasonable triager accept | BORDERLINE | Program rejects "descriptive error messages or headers." This returns a config JSON, not an error. But severity is low — server-observed IP, feature flags, geo data. |

**Verdict: HOLD**
- Reason: Disclosed data is server-side operational config, not customer/financial/auth data. Severity is low. Could be accepted as "information disclosure" or rejected as "not a security impact." Borderline.

---

## LEAD 7: `_api/socket.io` Anonymous Realtime Feed

**Finding:** `roobet.com/_api/socket.io` accepts anonymous engine.io polling handshake (Origin-gated but accepts `Origin: https://roobet.com`). Default namespace broadcasts live `new_bet`, `settingsUpdated`, withdraw flags to unauthenticated connections.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com API |
| Q2 Attacker-reachable | YES | Unauthenticated (Origin: roobet.com required but that's public) |
| Q3 Real security impact | LOW | Broadcast feed of aggregate bet data/settings; no user-specific data leaked |
| Q4 Provable non-invasively | YES | Polling handshake + subscribe |
| Q5 Novel/unreported | UNCERTAIN | Live bet feeds may be intentional public feature |
| Q6 Not always-rejected | YES | Not on rejection list |
| Q7 Reasonable triager accept | BORDERLINE | If bet feed is intentional public feature (common in crypto casinos), not a vulnerability. If intended to be auth-gated, it is. |

**Verdict: HOLD**
- Reason: Unclear if anonymous bet feed is intentional (many crypto casinos show public bet feeds). Needs clarification on whether this was designed to require auth. If intentional, not a vuln.

---

## LEAD 8: `fs.roobet.com` GCS Bucket

**Finding:** `fs.roobet.com` serves FullStory JS from GCS. Root redirects to fullstory.com. `?list-type=2` redirects to fullstory.com. No object enumeration possible.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | Roobet-operated subdomain |
| Q2 Attacker-reachable | YES | Publicly accessible |
| Q3 Real security impact | NO | Bucket serves only FullStory assets; redirects prevent enumeration; no sensitive data exposed |
| Q4 Provable non-invasively | YES | GET requests |
| Q5 Novel/unreported | N/A | No vulnerability to report |
| Q6 Not always-rejected | YES | N/A |
| Q7 Reasonable triager accept | NO | No misconfiguration found. Bucket is properly configured. |

**Verdict: INVALID**
- Reason: No misconfiguration. GCS bucket serves only intended FullStory assets. ListBucketResult redirects to fullstory.com. No object enumeration possible.

---

## LEAD 9: Client Bundle Token Leakage (ContentStack, VWO, BetBy, PaymentIQ)

**Finding:** SPA bundle exposes ROOBET_BETBY_BRAND_ID, ROOBET_PAYMENTIQ_MERCHANT_ID/ENV, ContentStack delivery+preview tokens, VWO key. Delivery tokens are public-by-design; preview token might read draft CMS content.

| Gate | Verdict | Reason |
|------|---------|--------|
| Q1 In-scope | YES | roobet.com |
| Q2 Attacker-reachable | YES | Client-side JS |
| Q3 Real security impact | NO | Delivery tokens public-by-design; preview token for draft marketing content only; no customer/financial/auth data |
| Q4 Provable non-invasively | YES | Bundle analysis |
| Q5 Novel/unreported | LOW | Public tokens are public by design |
| Q6 Not always-rejected | YES | Not explicitly listed, but program rejects "Exposure of customer/employee/financial/auth data during testing" and "Disclosure of known public files" |
| Q7 Reasonable triager accept | NO | ContentStack delivery tokens are public API keys. Preview token accesses marketing drafts, not security-sensitive data. Not a vulnerability. |

**Verdict: INVALID**
- Reason: Public-by-design tokens. No customer/financial/auth data exposure. Marketing CMS draft access is not a security vulnerability.

---

## Summary

| # | Lead | Verdict | CVSS |
|---|------|---------|------|
| 1 | GraphQL anonymous introspection (full schema + PII/financial field disclosure) | **VALID** | 5.3 |
| 2 | `liveRTPUpdate` pre-auth mutation execution | **HOLD** | — |
| 3 | Apollo GET CSRF bypass | **HOLD** | — |
| 4 | Admin endpoints on low-gate surface | **INVALID** | — |
| 5 | Affiliate endpoint IDOR hypothesis | **INVALID** | — |
| 6 | `_api/settings/get` config disclosure | **HOLD** | — |
| 7 | `_api/socket.io` anonymous feed | **HOLD** | — |
| 8 | `fs.roobet.com` GCS bucket | **INVALID** | — |
| 9 | Client bundle token leakage | **INVALID** | — |

**One VALID lead ready for submission.** The GraphQL introspection finding is concrete, non-invasively provable, and represents real API surface disclosure with PII/financial data model exposure. The HOLD leads need further investigation or have borderline severity that may not survive triager review.
