## 2026-09-07 06:13:41 UTC [target] (model bigpickle)
[HYP] Pre-auth GraphQL mutation execution + full schema disclosure (dual HTTP+WS)
class: OTHER
asset: roobet.com/_api/graphql
confidence: 95
reasoning: POST executes anonymous GraphQL (200 `__typename`); mutation validation passes pre-auth and reaches resolver (`liveRTPUpdate` → "No user with that id"); WS `graphql-transport-ws` accepts `connection_init` without `socketToken` giving full `__schema` (Q28/M26/S7); User type 64 fields incl. PII/financial/auth-tokens; HTTP GET transport enabled via `x-apollo-operation-name` header. Resolver data-plane enforces auth (NOT_AUTHENTICATED). Stable across all runs.
evidence_needed: none new — dual-channel repro fully characterized and reportable.
verify_steps: PASSIVE (all already confirmed): (1) POST `{"query":"mutation{liveRTPUpdate(data:{enabled:true}){__typename}}"}` → `"No user with that id"`/INTERNAL_SERVER_ERROR; (2) WS connection_init → __schema introspection; (3) HTTP GET with `x-apollo-operation-name` header executes query.
impact: Pre-auth disclosure of full API schema incl. 64-field User type (PII/financial/auth-token graph), 26 mutation input shapes, 7 subscription channels. Resolver auth caps standalone severity low; escalates to CRITICAL on session compromise. Reportable.
testability: PASSIVE
[HYP] Admin vertical priv-esc via normal-user session on low-gate admin routes
class: AUTH
asset: roobet.com/_api/admin/users
confidence: 55
reasoning: admin/users + admin/stats live (401) on low-gate surface, not behind Cloudflare bot-gate; Express connect.sid session auth; role-gate unverifiable without session; route stable across all probe dates (401, not 404).
evidence_needed: 200 JSON from /_api/admin/users with a normal-user session; OR sustained 401 ⇒ role-gated, close.
verify_steps: HUMAN (sandbox + normal session): GET /_api/admin/users ≤1 rps. 200 ⇒ CRITICAL.
impact: admin user listing (PII) + platform stats ⇒ HIGH.
testability: AUTH_HELPED
[HYP] Affiliate data IDOR via user_id/affiliate_code params
class: IDOR
asset: roobet.com/_api/affiliate/get
confidence: 45
reasoning: ?user_id=<id> and ?affiliate_code=<code> return 401 (not 404) — confirm param parsing pre-auth-gate; no cross-user data confirmed; re-probed 2026-09-06 stable.
evidence_needed: 200 with cross-user affiliate/commission rows.
verify_steps: HUMAN (two sandbox accounts): GET /_api/affiliate/get?user_id=<account_B> with session A ≤1 rps.
impact: cross-user affiliate/commission data ⇒ HIGH.
testability: AUTH_HELPED
[PARKED] None removed proactively — both session-gated survivors retain value and specific HUMAN verify steps; they await sandbox sessions rather than being dead-ends.
[FINAL] Survivors ranked: (1) Pre-auth GraphQL schema/mutation disclosure (95, PASSIVE, reportable), (2) Admin priv-esc via session (55, AUTH_HELPED), (3) Affiliate IDOR (45, AUTH_HELPED).
[NEXT] HUMAN: Submit the consolidated pre-auth finding to bugs.olivermaicher.eu — anonymous `graphql-transport-ws` transport (101 + connection_ack, no socketToken) + full introspection (Q28/M26/S7) + 64-field User type disclosure + mutation-validation error disclosure + HTTP POST mutation execution (`liveRTPUpdate` → "No user with that id") + HTTP GET transport (CSRF-bypassable via `x-apollo-operation-name`) at `wss://roobet.com/_api/graphql` + `https://roobet.com/_api/graphql`. This is the sole reportable robot-side finding; all remaining verified-value hypotheses are session-gated.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql HTTP GET: Apollo GET transport enabled — CSRF gate bypassable via x-apollo-operation-name header; same attack surface as POST but cacheable URL creates proxy/referrer leakage advantages. Low standalone severity.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql HTTP POST: mutation liveRTPUpdate → 200 "No user with that id" + INTERNAL_SERVER_ERROR — resolver executes pre-auth on HTTP channel. Earlier "400" observation was a no-body GET artifact.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql HTTP __type: INTROSPECTION_DISABLED on HTTP channel — `__type(name:"User")` blocked; only WS provides full introspection.
[LEARN] REJECTED OTHER @ roobet.com/_api/graphql HTTP GET __type: INTROSPECTION_DISABLED server-side; no partial type introspection via HTTP.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/*: zero surface delta on HTTP routes (6 live + 2 WS); remaining hypotheses session-gated.
[RISK] roobet: 55 — Pre-auth GraphQL schema disclosure finding (dual HTTP+WS) fully characterized and reportable (severity capped low by resolver auth on data-plane). Passive probing exhausted — surface stable at 6 HTTP + 2 WS routes with zero delta. All remaining high-value lines (admin priv-esc, affiliate IDOR) are session-gated → require HUMAN with sandbox sessions. No reportable vuln currently in hand confirmed as HIGH/CRITICAL; engagement rules respected (read-only GET/POST with body, ≤1 rps, no mutation execution on live customer data, no customer-data retention).
