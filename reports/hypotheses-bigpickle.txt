## 2026-09-12 00:37:52 UTC [target] (model bigpickle)
evidence_needed: A/B two sandbox accounts.
verify_steps: HUMAN sandboxed ≤1rps: session A → GET ?user_id=<acct_B> → 200 with B data ⇒ HIGH.
impact: Cross-user affiliate data disclosure; HIGH if 200.
testability: AUTH_HELPED
[PARKED] Admin priv-esc (55): session-cookie required; violates no-session-theft rule; no passive test possible.
[PARKED] Affiliate IDOR (45): A/B sandbox accounts required; AUTH_HELPED only.
[PARKED] graphql A/B ID-presence oracle / money-mutation fire test: requires throwaway live account + state-change risk; HUMAN sandboxed only.
[FINAL] Pre-auth GraphQL Disclosure (88, PASSIVE) — sole robot-side reportable line; fully characterized dual-channel; resolver auth caps severity at LOW-MEDIUM.
[NEXT] HUMAN: Submit pre-auth GraphQL disclosure package via bugs.olivermaicher.eu (WS anonymous graphql-transport-ws 101 + full introspection 28Q/26M/7S incl. 64-field User type; HTTP GET CSRF-bypassable Apollo transport; 7/7 mutations validate+execute pre-auth with resolver auth enforcing NOT_AUTHENTICATED on read-plane; note liveRTPUpdate hardening observed 09-08) — PoC read-only; all remaining high-value verifications (admin/users cookie test, affiliate A/B IDOR, money-mutation A/B) require sandboxed sessions, out of robot reach.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/*: zero surface delta re-confirmed this run — settings/get 35 keys ~byte-stable (1765B), admin/users 401, affiliate/get?user_id=1 401, graphql 400 no-body, socket.io Origin-gated polling 200.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql: transport re-confirmed live (HTTP 400 / polling 200 / WS 101); pre-auth GraphQL disclosure remains sole robot-side reportable line; resolver auth caps severity.
[RISK] roobet: 50 — Surface static (6 HTTP + 2 WS); server actively hardening (liveRTPUpdate msg suppressed, HTTP introspection disabled, config trimmed 68→35). Sole robot-side finding is low-severity pre-auth GraphQL disclosure; all HIGH/CRITICAL lines (admin priv-esc, affiliate IDOR, money-mutation) are session-gated. Read-only ≤1 rps, no state changes, no data retained; no further robot-side value — remaining steps are HUMAN submission + sandboxed-session tests.
[HYP] Pre-auth GraphQL Schema/Operation Disclosure on Dual HTTP+WS Channels (resolver auth-curtain degraded to uncaught ISE)
class: OTHER
asset: roobet.com/_api/graphql
confidence: 88
reasoning: Anonymous graphql-transport-ws (101 + connection_ack, no socketToken) + full introspection (28Q/26M/7S) WS-only; HTTP POST valid-body query executes resolvers pre-auth; `exchangeRates` read-query this run returns 2×INTERNAL_SERVER_ERROR on GET (x-apollo-operation-name bypass) AND POST — prior clean NOT_AUTHENTICATED response gone, resolver still fires anonymously but error path changed; 7/7 mutations previously confirmed executing pre-auth → ISE; User type 64 fields incl. KYC PII/financials/auth tokens.
evidence_needed: none robot-side — read-plane behavior now uniform ISE across channels; residual A/B value only.
verify_steps: [FINAL] read-only query/validation probes exhausted; transport re-confirmed live this run (GET 200/ISE, POST 200/ISE).
impact: Anonymous schema+operation disclosure; uniform pre-auth resolver execution on reads and writes; wrong-account targeted operations reachable by any registered user; LOW-MEDIUM standalone (no data plane leak), CRITICAL only downstream of session compromise.
testability: PASSIVE
[HYP] Admin Priv-esc via Normal-User Session on /_api/admin/users
class: AUTH
asset: roobet.com/_api/admin/users
confidence: 55
reasoning: 401 on low-gate surface (bypasses Cloudflare bot-gate); Express.js session auth (connect.sid HttpOnly); authorization vs session-validity indistinguishable anonymously; no A/B without normal-user cookie.
evidence_needed: 200/403 for a non-admin connect.sid.
verify_steps: HUMAN sandboxed ≤1rps: GET /_api/admin/users with normal-user cookie → 200 ⇒ CRITICAL.
impact: Cross-tenant user/PII dump; CRITICAL if 200.
testability: AUTH_HELPED
[HYP] Affiliate Endpoint IDOR via user_id Param
class: IDOR
asset: roobet.com/_api/affiliate/get
confidence: 45
reasoning: `?user_id`/`?affiliate_code` return 401 (not 404) across 10+ runs — parameter parsing alive; authorization unknown; no passive A/B.
evidence_needed: A/B two sandbox accounts.
verify_steps: HUMAN sandboxed ≤1rps: session A → GET ?user_id=<acct_B> → 200 with B data ⇒ HIGH.
impact: Cross-user affiliate data disclosure; HIGH if 200.
testability: AUTH_HELPED
[HYP] Pre-auth GraphQL Schema/Operation Disclosure on Dual HTTP+WS Channels (resolver auth-curtain degraded to uncaught ISE)
class: OTHER
asset: roobet.com/_api/graphql
confidence: 88
reasoning: Anonymous graphql-transport-ws (101 + connection_ack, no socketToken) + full introspection (28Q/26M/7S) WS-only; `__type` INTROSPECTION_DISABLED over HTTP; Apollo GET transport live with CSRF-preflight gate bypassable via `x-apollo-operation-name`; 7/7 mutations (liveRTPUpdate, updateUserProfile, placeBet, claimBonus, forfeitCashableBonus, updateEmail, updatePassword) pass validation and execute resolvers pre-auth → INTERNAL_SERVER_ERROR, not 401/NOT_AUTHENTICATED; User type exposes 64 fields incl. email, KYC PII, balances, socketToken/fasttrackToken; subscriptions kycUpdated/polymarketOrders leak `Cannot read properties of null (reading 'user')`.
evidence_needed: none robot-side — read-plane and write-plane exhaustively characterized; residual value only via A/B with sessions.
verify_steps: [FINAL] read-only introspection/validation/transport probes exhausted across WS+GET+POST; transport re-confirmed live this run (400 no-body on POST, polling 200, WS 101).
impact: Anonymous schema+operation+error disclosure enabling any registered user to target wrong-account operations; LOW-MEDIUM standalone (resolver `NOT_AUTHENTICATED` caps data-plane), CRITICAL only downstream of session compromise; valid-bugs.md already marks the introspection line VALID 5.3.
testability: PASSIVE
[HYP] Admin Priv-esc via Normal-User Session on /_api/admin/users
class: AUTH
asset: roobet.com/_api/admin/users
confidence: 55
reasoning: 401 on low-gate surface (bypasses Cloudflare bot-gate); Express.js session auth (`connect.sid` HttpOnly + `userId`); authorization-vs-session-validity indistinguishable anonymously.
evidence_needed: 200/403 for a non-admin `connect.sid`.
verify_steps: HUMAN sandboxed ≤1rps: GET /_api/admin/users with normal-user cookie → 200 ⇒ CRITICAL.
impact: Cross-tenant user/PII dump; CRITICAL if 200.
testability: AUTH_HELPED
[HYP] Affiliate Endpoint IDOR via user_id Param
class: IDOR
asset: roobet.com/_api/affiliate/get
confidence: 45
reasoning: `?user_id`/`?affiliate_code` return 401 (not 404) across 10+ runs — parameter parsing alive; authorization unknown.
evidence_needed: A/B two sandbox accounts.
verify_steps: HUMAN sandboxed ≤1rps: session A → GET ?user_id=<acct_B> → 200 with B data ⇒ HIGH.
impact: Cross-user affiliate data disclosure; HIGH if 200.
testability: AUTH_HELPED
[NEXT] HUMAN: Submit pre-auth GraphQL disclosure package at bugs.olivermaicher.eu — WS anonymous graphql-transport-ws 101 + full introspection (28Q/26M/7S incl. 64-field User type with email/KYC/financial/auth-token fields); HTTP GET Apollo transport CSRF-gate bypass (`x-apollo-operation-name`); 7/7 mutations pass validation + execute resolvers pre-auth (uncaught ISE, not NOT_AUTHENTICATED); note 09-08 hardening (liveRTPUpdate message suppressed, HTTP `__type` disabled). PoC is read-only; all residual HIGH/CRITICAL verifications (admin/users cookie, affiliate A/B IDOR, money-mutation A/B) require sandboxed sessions — out of robot reach; further passive probes add zero information on a static surface.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/*: zero surface delta re-confirmed this run — _api/graphql 400, admin/users+stats 401, socket.io 400, root 200 byte-consistent with prior 10+ runs.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql: pre-auth GraphQL disclosure remains sole robot-side reportable line (already VALID 5.3 in valid-bugs.md); resolver auth caps severity; transport live WS 101 / HTTP 400.
[LEARN] ACCEPTED MISCONFIG @ roobet.com: server actively hardening (liveRTPUpdate msg suppressed, HTTP introspection disabled, settings/get trimmed 68→35); surface otherwise static.
[RISK] roobet: 50 — Surface static (6 HTTP + 2 WS); server actively hardening; sole robot-side finding is low-severity pre-auth GraphQL disclosure (already validated 5.3); all HIGH/CRITICAL lines (admin priv-esc, affiliate IDOR, money-mutation A/B) session-gated and out of robot reach. Read-only ≤1 rps, no state changes, no data retained; remaining value is HUMAN submission + sandboxed-session testing.
