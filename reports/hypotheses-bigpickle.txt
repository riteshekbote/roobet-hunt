## 2026-09-09 01:06:54 UTC [target] (model bigpickle)
[HYP] Pre-auth GraphQL schema/mutation/User-type disclosure via dual HTTP+WS channel
class: OTHER
asset: roobet.com/_api/graphql
confidence: 95
reasoning: Anonymous graphql-transport-ws (101 + connection_ack, no socketToken) + full introspection (Query 28/Mut 26/Sub 7) incl. 64-field User type (email, KYC PII, financial, auth tokens, role flags); all 26 mutations pass validation anonymously leaking InputType/enum/scalar shapes; HTTP GET transport enabled (CSRF gate bypassed via x-apollo-operation-name); INTROSPECTION_DISABLED on HTTP only; liveRTPUpdate resolver executes pre-auth on HTTP (now 400+3×INTERNAL_SERVER_ERROR — server patched message but resolver still runs, not 401/NOT_AUTHENTICATED).
evidence_needed: None — fully characterized; sole robot-side reportable line.
verify_steps: HUMAN: submit to bugs.olivermaicher.eu with WS introspection output, HTTP GET CSRF-gate bypass, mutation validation error disclosure, liveRTPUpdate pre-auth execution proof.
impact: schema/PII-structure disclosure + ID-oracle priming; low standalone severity (resolver auth caps data-plane).
testability: PASSIVE
[PARKED] Admin priv-esc via normal-user session (AUTH_HELPED): requires live session; no passive path.
[PARKED] Affiliate IDOR via user_id params (AUTH_HELPED): requires two sandbox accounts.
[PARKED] Game-provider ID oracle liveRTPUpdate: POST excluded from read-only envelope; cannot A/B differentiate passively. No state change issued per engagement rules.
[FINAL] Sole survivor: pre-auth GraphQL disclosure (95, OTHER, PASSIVE) — reportable.
[NEXT] HUMAN: Submit fully-characterized dual-channel pre-auth GraphQL disclosure to bugs.olivermaicher.eu (WS introspection Query28/Mut26/Sub7 + 64-field User PII graph + mutation validation error disclosure + HTTP GET CSRF-gate bypass via x-apollo-operation-name + liveRTPUpdate pre-auth execution 400+3×INTERNAL_SERVER_ERROR + INTROSPECTION_DISABLED-on-HTTP asymmetry), severity low. Same sandboxed session (≤1 rps), test GET /_api/admin/users (200 ⇒ CRITICAL) and GET /_api/affiliate/get?user_id=<acct_B> with session A (200 ⇒ HIGH).
[LEARN] ACCEPTED OTHER @ roobet.com/_api/graphql: Zero delta across runs; dual-channel pre-auth GraphQL disclosure remains the sole reportable line; resolver-level NOT_AUTHENTICATED caps severity.
[LEARN] ACCEPTED OTHER @ roobet.com/_api/*: Surface static (6 HTTP + 2 WS); config dump stable (35 keys), liveRTPUpdate error message leak patched (400+3×INTERNAL_SERVER_ERROR); remaining high-value lines session-gated.
[RISK] roobet: 55 — Passive surface fully bounded with zero delta across consecutive runs (6 HTTP + 2 WS routes; server actively patching disclosure vectors — liveRTPUpdate message suppressed, introspection HTTP-disabled, config dump trimmed 68→35). Sole robot-side reportable finding is the low-severity dual-channel pre-auth GraphQL schema+mutation disclosure. All HIGH/CRITICAL lines (admin priv-esc, affiliate IDOR, ID oracle A/B) require a live session or exceed the read-only POST envelope. Engagement rules respected (read-only GET/HEAD/OPTIONS, server-observed egress only, no state changes, no customer data retained). Next value only from HUMAN submission + sandboxed-session tests.
